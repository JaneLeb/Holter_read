# ПЛАН И ПОЛЕЗНЫЕ МАТЕРИАЛЫ ДЛЯ C++ РАЗРАБОТЧИКА

### 🔧Архитектурная роль и стратегия для C++ ядра: как обойти конкурентов

Самый главный инсайт, который лежит на стыке азработчика C++ и специфики медицинского ИИ в России звучит так: «Чистый сигнал дороже самой умной нейросети».


В Data Science есть закон: «Мусор на входе — мусор на выходе» (Garbage In, Garbage Out). И вот в чём кроется скрытая техническая драма, которая рушит 90% медицинских стартапов:


Нейросети не умеют работать с «грязным» Холтером. Большинство DS-специалистов обучают свои модели на идеальных, чистых датасетах (вроде MIT-BIH), которые записывались в стационарах на идеальном оборудовании.
Но в реальной жизни пациент с Холтером:


* Чешет грудь (электрический шум).


* Идет быстрым шагом (дрейф изолинии — график начинает волнообразно прыгать вверх-вниз).


* Потеет (сигнал блекнет).


Если скормить этот реальный суточный график обычной нейросети, она сойдет с ума и выдаст сотни ложных аритмий. C++ фильтр — это не просто вспомогательная утилита, это главный элемент жизнеспособности ИИ. Если написать алгоритм, который ювелирно отсекает мышечные артефакты и выравнивает дрейфующую изолинию до того, как данные попадут в ИИ, это сделает 70% успеха всего продукта.


Ядро должно выполнять роль высокопроизводительного сервиса первичной обработки (Heavy Lifter):
  
  * Парсинг:
    Суточная запись Холтера — это огромный бинарный файл (в форматах EDF, MIT-BIH или закрытых вендорских форматах вроде Schiller/Incart). C++ парсер мгновенно читает этот массив в оперативную память.
  
    
  * Фильтрация и Детекция (Пан-Томпкинс):
  Код на C++ очищает сигнал от изоэлектрического дрейфа (дыхания) и шумов, а затем находит все R-зубцы (QRS-комплексы). На выходе - точнейшая сетка кардиоинтервалов (RR-интервалы) и карта нарушений ритма.

Алгоритм Пана-Томпкинса (Pan-Tompkins) — это классика кардиоанализа. Он работает на жесткой математике (фильтры, дифференцирование, возведение в квадрат, адаптивные пороги) и идеально подходит для C++, так как требует минимальных ресурсов и работает молниеносно. 

Фильтр опасныех участков согласно типам активностей из дневника (возможно пока достаточно спорта и сна) или другие критически важные на этапе MVP ( например - бег + сердце билось часто = норма, а покой + сердце билось часто = патология).

  * Передача данных для анализа ИИ. Data Science модель должна получать не только отфильтрованные данные, но и контекст (например, отправлять участок ЭКГ с пометкой «пациент сидел, но пульс подскочил».)

✔️ОБЩАЯ ИДЕЯ: Написать C++ обертку, которая с помощью EDFLib читает файл, кусками (окнами) передает его в ваш доработанный Пан-Томпкинс и на выходе выдает структурированный массив RR-интервалов (таймстампы каждого удара сердца).
«Обнаружено 15 эпизодов тахикардии, максимальная пауза — 2.5 сек в 03:40 ночи». 
Врач сразу перейдет к нужным минутам, а не будет листать 24 часа записи. Архитектура MVP может быть собрана как конструктор имеющихся на рынке и доработаных open-sourse решений. 


### ♻️ Как связать C++ с Python-инфраструктурой команды
Чтобы Data Scientist'ы и разработчики ИИ могли использовать надежное ядро, оформляем его в виде Python-библиотеки. Для C++ разработчика это стандартный шаг:

* [pybind11](https://github.com/pybind/pybind11) или Cython: оборачиваем C++ код алгоритма Пана-Томпкинса в легковесную обертку.

  
* Результат: питонисты пишут в своем коде всего одну строчку: import my_cpp_core и вызывают С++ функцию детекции. При этом код работает со скоростью чистого C++, а нейросети получают идеально подготовленные и «нарезанные» алгоритмом данные.


## 🐾 Что такое «Пайплайн обработки данных в консоли»?
Разработка всегда начинается с консольного пайплайна (бизнес-логики). Пока код не научится правильно обрабатывать цифры в памяти, делать графический интерфейс (кнопочки и графики) нет смысла.

Пайплайн (Pipeline — конвейер) — это последовательность шагов, через которую проходят данные от начала до конца.
В C++ для проекта это означает программу без графического интерфейса (черное окошко консоли), которая работает по строгой цепочке, шаг за шагом:


###  📤3.Передача кода в Python:
План действий для вас (С++ разработчика) по стыковке с Питоном:


Разработчик ядра, может лично проконтролировать, чтобы архитектурных косяков не случилось. Для этого можно использовать динамическую библиотеку(.so / .dll). Разница между консольным приложением и динамической библиотекой минимальна. Вместо функции int main() вы просто экспортируете наружу несколько функций, которые будет вызывать Python.
1.  Используйте `pybind11`: Это C++ библиотека, состоящая только из заголовочных файлов (`header-only`). Вам не нужно ничего сложно настраивать в компиляторе. Вы пишете свой привычный C++ код, а в конце файла добавляете небольшой блок, который «показывает» ваши функции Питону.


2.  Как это выглядит в коде:

    cpp

    ```
        #include <pybind11/pybind11.h>
        #include <pybind11/stl.h> // Автоматом переводит std::vector в списки Python
    
        // Ваша функция фильтрации
        std::vector<float> filter_holter(const std::string& filepath, const std::string& diarypath) {
            // Ваш крутой алгоритм на C++
            return cleaned_data;
        }
    
        // Блок pybind11 для сборки модуля
        PYBIND11_MODULE(holter_core, m) {
            m.def("filter_holter", &filter_holter, "Функция очистки и мэтчинга");
        }

    ```



3.  Результат: После компиляции вы отдаете вашему Python-разработчику файл с расширением `.so` (для Linux/macOS) или `.pyd` (для Windows). Он кладет его в папку со своим FastAPI проектом и пишет:

    python

    ```
        import holter_core
        # Вызывает вашу С++ функцию прямо из Питона!
        clean_signal = holter_core.filter_holter("record.dat", "diary.csv")

    ```
На чем запускать нейросеть?
Если вы обучите модель в Python (например, в TensorFlow или PyTorch), тащить весь Python в проект на C++ — плохая идея. Используйте легковесные движки вывода (Inference Engines):

* [ONNX Runtime (C++ API)](https://onnxruntime.ai/): Самый популярный путь. Экспортируете вашу нейросеть из Python в формат .onnx, а в C++ коде подключаете библиотеку ONNX Runtime. Она работает молниеносно и ест копейки ОЗУ.
* TensorFlow Lite (C++ API): Тоже отличный выбор, особенно если в будущем захотите запустить ваш алгоритм внутри Android/iOS приложения для той самой кардиофлешки.

### 4. 📤Выход - визуализация (Интерфейс для врача)
Результат для врача. Программа выдает текстовый отчет. На диске сохраняется готовый красивый текстовый файл report.txt, с итоговым результатом программы (MVP) - коротким и понятным отчетом для врача:

   1. Сколько всего секунд прибор записал (длительность исследования).

      
   2. Сколько раз за сутки провод отходил (ШУМ).

      
   3. В какие конкретно секунды произошла КРИТИЧЕСКАЯ ПАУЗА (чтобы врач не листал все 24 часа, а сразу открыл эти секунды).


Например:


«Пациент Иванов. Дневной пульс: 75. Ночной: 55. Обнаружено 3 паузы более 2 секунд в период с 03:15 по 03:17»



Так же в С++ возможно использовать инструмент Qt Framework (стандарт для C++ GUI). Он активно используется в индустрии
и может независимо от питонистов отображать графики и подсвечивать "опасные" зоны красным цветом.




Первый шаг  — не написать идеальный софт, а просто скомпилировать первый код, который прочитает тестовый файл и отсечет лишнее.
Вот ваш пошаговый план 

:
## День 1–2: Готовим полигон и скачиваем тестовое «сердце»
Вам нужен реальный файл ЭКГ в международном формате EDF, на котором вы будете тренироваться.

   1. Зайдите на PhysioNet в базу данных [PTB-XL](https://physionet.org/content/ptb-xl/1.0.3/) (это крупнейший открытый архив ЭКГ).
   2. Скачайте любой тестовый файл с расширением .edf (если скачался архив, распакуйте его). Это суточная или краткосрочная запись реального пациента.
   3. Установите на компьютер бесплатную программу [EDFbrowser](https://www.teuniz.net/edfbrowser/). Откройте в ней скачанный файл. Вы увидите графики ЭКГ. Запомните, как они выглядят «глазами» — к этому результату должен прийти ваш код.



## День 3–4: Учим C++ читать медицинский файл
Теперь заставим C++ открыть этот бинарник. Нам нужен парсер.

   1. Скачайте с GitHub или официального сайта библиотеку [EDFLib](https://www.teuniz.net/edflib/). Она состоит всего из двух файлов: edflib.c и edflib.h.
   2. Создайте пустой C++ проект в вашей любимой среде (VS Code, Visual Studio или CLion) и просто закиньте эти два файла в папку с проектом.
   3. Напишите простейшую функцию main, которая вызывает edf_open_file. Скопируйте базовый пример из документации библиотеки, чтобы ваш код просто вывел в консоль: «Файл успешно открыт. Частота дискретизации: 250 Гц. Количество каналов: 3». Как только цифры в консоли совпадут с тем, что показывал EDFbrowser — первый рубеж взят!



## День 5: Подключаем фильтр шумов
Файл прочитан в массив чисел. Теперь уберем сетевую наводку (гул от розеток 50 Гц, который есть в любом Холтере).

   1. Скачайте легковесную C++ библиотеку [IIR1](https://github.com/berndporr/iir1).
   2. Нам нужен режекторный фильтр (Notch Filter). Добавьте в свой код инициализацию фильтра на 50 Гц.
   3. Запустите цикл for по вашему массиву данных ЭКГ и пропустите каждое число через функцию .filter(). Результат (очищенные числа) запишите в новый массив.



## День 6: Проверка результата
Как понять, что код новичка сработал и шум исчез?

   1. Используйте ту же библиотеку EDFLib, чтобы сохранить ваш новый очищенный массив обратно в новый файл (например, clean_output.edf). Для этого там есть функция edf_write_physical_samples.
   2. Откройте ваш clean_output.edf в программе EDFbrowser и положите рядом с исходным «грязным» файлом. Вы физически увидите, что линия графика стала ровной, а мелкая «дрожь» от розеток исчезла.



## День 7: Результат: 

Вы только что сделали то, что делают коммерческие медицинские плагины.  

C++ парсер и первый контур фильтрации готовы. Сигнал очищается. Для нейросети упаковываем код в pybind11

.
------------------------------
С этим результатом вы уже можете уверенно подавать документы в Московский инновационный кластер (МИК). Эксперты в акселераторах оценивают не объем кода, а динамику. Если вы покажете, что разобрались с архитектурой EDF и фильтрацией, вам с радостью дадут ментора-кардиолога, который поможет докрутить логику шумов.

## День 8: Пишем классический класс-парсер на C++. 

Настоящие данные ЭКГ и Холтера (например, из той самой открытой базы MIT-BIH) обычно хранятся в простейших файлах .csv или .txt.
Внутри такого файла лежит просто бесконечный столбец чисел — показания электрического потенциала сердца, зафиксированные прибором через равные доли секунды.
Вот готовый код класса HolterDataReader. Он открывает файл, читает эти числа «пачками» по 187 штук (размер одного удара сердца) и передает их в массив, который готов к отправке в ИИ [stem-calculative-problem-solving].


## Код парсера ЭКГ-файлов на C++
      
      
      #include <iostream>#include <fstream>#include <vector>#include <string>
      class HolterDataReader {private:
          std::string file_path;
      public:
          // Конструктор: запоминаем путь к файлу с данными Холтера
          HolterDataReader(const std::string& path) : file_path(path) {}
      
          // Функция чтения: вырезает из файла следующий удар сердца (187 чисел)
          std::vector<float> get_next_heartbeat(std::ifstream& file, bool& has_more_data) {
              std::vector<float> heartbeat;
              std::string line;
      
              // Читаем ровно 187 строк (точек) из файла
              while (heartbeat.size() < 187 && std::getline(file, line)) {
                  if (!line.empty()) {
                      // Переводим строчку текста в число типа float и кладем в массив
                      heartbeat.push_back(std::stof(line));
                  }
              }
      
              // Проверяем, не закончился ли файл целиком
              if (file.peek() == EOF) {
                  has_more_data = false;
              } else {
                  has_more_data = true;
              }
      
              // Если файл внезапно оборвался и мы не наскребли 187 точек,
              // добиваем массив нулями, чтобы ИИ не выдал ошибку из-за размера
              if (heartbeat.size() < 187 && !heartbeat.empty()) {
                  heartbeat.resize(187, 0.0f);
              }
      
              return heartbeat;
          }
      };

## Как это теперь объединить в функции main?
Теперь мы можем связать ваш парсер и модуль ИИ вместе. Программа будет открывать файл, по очереди доставать из него каждый удар сердца и сканировать его нейросетью:

    int main() {
        // 1. Инициализируем ИИ и парсер
        AIAnalyzer holter_ai("mit_bih_arrhythmia.onnx");
        HolterDataReader reader("patient_data.txt"); // Файл с кардиограммой пациента
    
        // 2. Открываем файл для чтения
        std::ifstream file("patient_data.txt");
        if (!file.is_open()) {
            std::cerr << "Ошибка: Не удалось открыть файл с данными пациента!" << std::endl;
            return 1;
        }
    
        bool has_data = true;
        int heartbeat_counter = 1;
    
        std::cout << "[Старт]: Начинаем полный анализ суточного Холтера..." << std::endl;
    
        // 3. Крутим цикл, пока в файле есть данные
        while (has_data) {
            // Парсер достает порцию из 187 чисел
            std::vector<float> current_beat = reader.get_next_heartbeat(file, has_data);
           
            if (current_beat.empty()) break;
    
            // ИИ анализирует эту порцию
            std::string verdict = holter_ai.analyze_heartbeat(current_beat);
    
            // Если ИИ нашел аритмию — выводим в консоль номер плохого удара сердца
            if (verdict.find("ВНИМАНИЕ") != std::string::npos) {
                std::cout << "Удар №" << heartbeat_counter << " -> " << verdict << std::endl;
            }
    
            heartbeat_counter++;
        }
    
        std::cout << "[Финиш]: Анализ завершен. Проверено ударов сердца: " << heartbeat_counter << std::endl;
        file.close();
        return 0;
    }

## Как будет выглядеть ваш файл patient_data.txt?
Вы можете создать его сами для теста в Блокноте. Просто напишите туда в столбик любые 187 чисел (например, от 0.0 до 1.0) [stem-calculative-problem-solving]. Ваш парсер считает их, превратит в массив, а ИИ выдаст вердикт.
Вот и всё! Вы только что на пальцах спроектировали архитектуру настоящего медицинского софта. Это уже не «прожект», а готовая логика инженерного продукта.
Если хотите сделать этот тест реальным, мне подсказать вам, где на Hugging Face или PhysioNet взять настоящий текстовый файл с ЭКГ живого человека, чтобы скормить его вашей программе?

Чтобы провести настоящий тест вашего кода, вам нужны реальные сигналы ЭКГ живых людей. Самый удобный и простой способ получить их в виде текстовых файлов — скачать очищенную версию базы данных MIT-BIH Arrhythmia Database с платформы Kaggle, где они уже переведены в формат простых таблиц. [1, 2]
## Где именно взять файлы:

   1. Зайдите на сайт Kaggle (зарегистрируйтесь, если еще нет).
   2. Вбейте в поиск: MIT-BIH Arrhythmia Database (Simple CSVs) (автор датасета: protobioengineering).
   3. Скачайте архив. Внутри вы найдете файлы с именами вроде 100_ekg.csv или 101_ekg.csv. [1, 3]

## Как устроен этот файл внутри:
Если вы откроете скачанный файл 100_ekg.csv через обычный Блокнот, вы увидите структуру, с которой ваш написанный С++ парсер справится без труда: [4]

0.125, -0.120
0.130, -0.110
0.145, -0.095

Каждая строчка — это зафиксированный датчиком электрический сигнал сердца в конкретную миллисекунду. [4]
## Как провести первый тест:

   1. Переименуйте любой скачанный файл в patient_data.txt и положите его в папку к вашей скомпилированной программе.
   2. Запустите ваш С++ код.
   3. Класс-ридер начнет пачками по 187 строк забирать эти цифры, а модель ИИ с Hugging Face начнет выдавать реальные диагнозы для этих данных.

Вы увидите, как на ваших глазах сухие цифры из файла превращаются в автоматические медицинские предупреждения. 


## День 8: Вывод графиков кардиограммы на экран для врача и сохранение результатов анализа в отдельный итоговый файл

В коммерческом софте это называется разделением на логику, хранение и интерфейс. Мы добавим в нашу систему два новых независимых класса: ReportSaver (для сохранения текстовых отчетов на диск) и Visualizer (для вывода графика прямо в консоль)

.
------------------------------
### Шаг 1. Класс ReportSaver (Сохранение в файл)
Этот класс отвечает за запись результатов в файл .txt или .csv, чтобы врач мог открыть его на любом компьютере, распечатать или прикрепить к медицинской карте пациента.

      #include <fstream>#include <chrono>#include <iomanip>
      class ReportSaver {private:
          std::string output_path;
          std::ofstream report_file;
      public:
          ReportSaver(const std::string& path) : output_path(path) {
              report_file.open(output_path, std::ios::app); // Открываем файл в режиме добавления
              if (report_file.is_open()) {
                  report_file << "=== НАЧАЛО МЕДИЦИНСКОГО ОТЧЕТА ХОЛТЕРА ===\n";
              }
          }
      
          ~ReportSaver() {
              if (report_file.is_open()) {
                  report_file << "=== КОНЕЦ ОТЧЕТА ===\n";
                  report_file.close();
              }
          }
      
          // Запись аномалии с указанием номера удара
          void log_anomaly(int beat_number, const std::string& verdict) {
              if (report_file.is_open()) {
                  report_file << "[Удар №" << beat_number << "] " << verdict << "\n";
              }
          }
      };

------------------------------
### Шаг 2. Класс Visualizer (График прямо в консоли!)
Полноценный графический интерфейс (GUI) на С++ требует тяжелых библиотек (типа Qt), что сложно для первого пэт-проекта. Но мы можем сделать псевдографику прямо в консоли с помощью обычных текстовых символов * или #!
Этот класс будет брать 187 точек удара сердца, масштабировать их и «рисовать» график ЭКГ прямо в терминале Школы 21.

      #include <algorithm>
      class Visualizer {public:
          // Рисует один удар сердца в консоли
          void draw_heartbeat(const std::vector<float>& heartbeat_data, int width = 60, int height = 15) {
              if (heartbeat_data.empty()) return;
      
              std::cout << "\n--- График подозрительного удара сердца ---\n";
      
              // Находим минимум и максимум для масштабирования графика по вертикали
              auto [min_it, max_it] = std::minmax_element(heartbeat_data.begin(), heartbeat_data.end());
              float min_val = *min_it;
              float max_val = *max_it;
              float range = max_val - min_val;
              if (range == 0.0f) range = 1.0f;
      
              // Идем сверху вниз по строкам экрана
              for (int r = height; r >= 0; --r) {
                  float current_y = min_val + (static_cast<float>(r) / height) * range;
                 
                  // Идем слева направо по шагам данных
                  for (int c = 0; c < width; ++c) {
                      // Масштабируем индекс массива под ширину экрана
                      int data_idx = (c * heartbeat_data.size()) / width;
                      float val = heartbeat_data[data_idx];
      
                      // Если значение близко к текущей строке — рисуем точку графика
                      if (std::abs(val - current_y) < (range / (height * 2.0f))) {
                          std::cout << "#";
                      } else if (c == 0) {
                          std::cout << "|"; // Ось Y
                      } else {
                          std::cout << " ";
                      }
                  }
                  std::cout << "\n";
              }
              std::cout << std::string(width, '-') << "\n"; // Ось X
          }
      };

------------------------------
### Шаг 3. Объединяем всё в единую систему
Теперь перепишем нашу главную функцию main. Программа будет читать файл, проверять данные через ИИ, при обнаружении аритмии автоматически записывать строчку в файл отчета и выводить график этого сбоя на экран для врача.

      int main() {
          // Инициализируем все модули нашей системы
          AIAnalyzer holter_ai("mit_bih_arrhythmia.onnx");
          HolterDataReader reader("patient_data.txt");
          ReportSaver saver("final_report.txt"); // Сюда запишется отчет
          Visualizer visualizer;                 // Сюда выведется график
      
          std::ifstream file("patient_data.txt");
          if (!file.is_open()) {
              std::cerr << "Ошибка открытия файла данных!" << std::endl;
              return 1;
          }
      
          bool has_data = true;
          int heartbeat_counter = 1;
      
          std::cout << "[Система запущена]: Сканирование кардиограммы..." << std::endl;
      
          while (has_data) {
              std::vector<float> current_beat = reader.get_next_heartbeat(file, has_data);
              if (current_beat.empty()) break;
      
              std::string verdict = holter_ai.analyze_heartbeat(current_beat);
      
              // Если ИИ бьет тревогу
              if (verdict.find("ВНИМАНИЕ") != std::string::npos) {
                  std::cout << "\n[!] Обнаружена патология в ударе №" << heartbeat_counter << std::endl;
                 
                  // 1. Сохраняем в файл на диске
                  saver.log_anomaly(heartbeat_counter, verdict);
                 
                  // 2. Рисуем график сбоя прямо в терминале
                  visualizer.draw_heartbeat(current_beat);
              }
      
              heartbeat_counter++;
          }
      
          std::cout << "\n[Готово]: Отчет сохранен в файл final_report.txt" << std::endl;
          file.close();
          return 0;
      }

### Итог
Это полноценный прототип медицинского софта. В нем реализованы принципы ООП, работа с файловой системой (I/O), интеграция сторонней библиотеки машинного обучения (ONNX) и базовый интерфейс (CLI-визуализация). 


## День 9. Пишем инструкцию по установке vcpkg, чтобы подключить ИИ без ошибок или создадим шаблон класса для обработки ошибок, если файл пациента поврежден

В медицине обработка ошибок — это критически важная часть. Если файл поврежден, данные искажены или в них пропущены строки, классическая программа упадет в сегфолт (segmentation fault), а реальный прибор в больнице просто выключится, что недопустимо.
В С++ для этого используется механизм исключений (exceptions). Мы создадим собственный класс ошибки HolterException, а также добавим в наш парсер систему «умной проверки» (валидации) данных на лету


------------------------------
### Шаг 1. Создаем класс медицинской ошибки HolterException
Этот класс наследуется от стандартного std::runtime_error. Он будет собирать информацию о том, в каком именно месте и почему произошел сбой.

    #include <stdexcept>#include <string>
    class HolterException : public std::runtime_error {private:
        std::string error_details;
    public:
        // Конструктор принимает сообщение и контекст (например, имя файла или номер строки)
        HolterException(const std::string& message, const std::string& context)
            : std::runtime_error(message), error_details(context) {}
    
        // Метод для получения подробностей ошибки для логов
        std::string get_details() const {
            return "Контекст ошибки: " + error_details;
        }
    };

------------------------------
### Шаг 2. Добавляем «умную проверку» в парсер HolterDataReader
Теперь перепишем функцию чтения данных. Если в файле окажутся битые данные (например, вместо чисел будет написан текст «ошибка датчика», или числа будут физически невозможными для ЭКГ), наш парсер не упадет, а выбросит исключение.

    #include <iostream>#include <fstream>#include <vector>#include <string>
    class HolterDataReader {private:
        std::string file_path;
        int line_counter = 0; // Считаем строки, чтобы точно знать, где ошибка
    public:
        HolterDataReader(const std::string& path) : file_path(path) {}
    
        std::vector<float> get_next_heartbeat(std::ifstream& file, bool& has_more_data) {
            std::vector<float> heartbeat;
            std::string line;
    
            while (heartbeat.size() < 187 && std::getline(file, line)) {
                line_counter++;
                if (line.empty()) continue;
    
                try {
                    // Пытаемся конвертировать строку в число
                    float val = std::stof(line);
    
                    // ВАЛИДАЦИЯ: Физический контроль данных (по Сатурну)
                    // Вольтаж ЭКГ обычно лежит в пределах от -5.0 до 5.0 мВ.
                    // Если там число 999.0 — это явный сбой оборудования.
                    if (val < -10.0f || val > 10.0f) {
                        throw HolterException(
                            "Критический всплеск напряжения (аномальный вольтаж)",
                            "Файл: " + file_path + ", Строка: " + std::to_string(line_counter) + ", Значение: " + line
                        );
                    }
    
                    heartbeat.push_back(val);
    
                } catch (const std::invalid_argument&) {
                    // Если stof не смог перевести текст в число (например, в файле написано "NULL" или буквы)
                    throw HolterException(
                        "Поврежденный формат данных (невозможно прочитать число)",
                        "Файл: " + file_path + ", Строка: " + std::to_string(line_counter) + ", Текст: '" + line + "'"
                    );
                }
            }
    
            if (file.peek() == EOF) {
                has_more_data = false;
            } else {
                has_more_data = true;
            }
    
            if (heartbeat.size() < 187 && !heartbeat.empty()) {
                heartbeat.resize(187, 0.0f);
            }
    
            return heartbeat;
        }
    };

------------------------------
### Шаг 3. Защищаем главный цикл программы с помощью try - catch
Теперь мы оборачиваем наш главный цикл в блок try-catch. Если файл окажется идеальным — программа отработает как обычно. Если внутри файла обнаружится текстовый мусор или безумные цифры — программа не упадет, а аккуратно запишет ошибку в лог и продолжит работу или штатно завершится.

    int main() {
        try {
            AIAnalyzer holter_ai("mit_bih_arrhythmia.onnx");
            HolterDataReader reader("patient_data.txt");
            ReportSaver saver("final_report.txt");
            Visualizer visualizer;
    
            std::ifstream file("patient_data.txt");
            if (!file.is_open()) {
                // Бросаем системное исключение, если файла вообще нет на диске
                throw std::runtime_error("Файл 'patient_data.txt' не найден в папке проекта!");
            }
    
            bool has_data = true;
            int heartbeat_counter = 1;
    
            std::cout << "[Система]: Сканирование запущено..." << std::endl;
    
            while (has_data) {
                // Метод ридера может выбросить HolterException, если наткнется на битые данные
                std::vector<float> current_beat = reader.get_next_heartbeat(file, has_data);
                if (current_beat.empty()) break;
    
                std::string verdict = holter_ai.analyze_heartbeat(current_beat);
    
                if (verdict.find("ВНИМАНИЕ") != std::string::npos) {
                    std::cout << "\n[!] Патология в ударе №" << heartbeat_counter << std::endl;
                    saver.log_anomaly(heartbeat_counter, verdict);
                    visualizer.draw_heartbeat(current_beat);
                }
    
                heartbeat_counter++;
            }
    
            std::cout << "\n[Успех]: Анализ завершен без критических сбоев." << std::endl;
            file.close();
    
        }
        // Ловим наши кастомные медицинские ошибки
        catch (const HolterException& ex) {
            std::cerr << "\n[КРИТИЧЕСКАЯ ОШИБКА ДАННЫХ]: " << ex.what() << std::endl;
            std::cerr << ex.get_details() << std::endl;
            std::cerr << "[Действие]: Замените кабель Холтера или проверьте исходный файл." << std::endl;
            return 2;
        }
        // Ловим любые другие стандартные ошибки С++ (например, нехватку памяти)
        catch (const std::exception& ex) {
            std::cerr << "\n[СИСТЕМНЫЙ СБОЙ]: " << ex.what() << std::endl;
            return 3;
        }
    
        return 0;
    }

### Итог
Теперь ваша программа обладает промышленным уровнем надежности. Она защищена от «грязных» данных. Пункт «Обработка исключительных ситуаций» уже полностью закрыт кодом.


## День 10. Последний шаг, чтобы запустить это на компьютере: посмотрим инструкцию по развертыванию vcpkg, чтобы CMake без проблем подтянул библиотеку ONNX

Чтобы подключить библиотеку машинного обучения ONNX Runtime на вашей рабочей машине с помощью пакетного менеджера vcpkg, вам понадобится выполнить несколько стандартных команд в терминале.
Права администратора часто ограничены, и этот способ идеален — он разворачивает все библиотеки локально, прямо в вашей домашней папке, не требуя системных паролей sudo

.
------------------------------
### Шаг 1. Клонируем и настраиваем vcpkg (если его еще нет)
Откройте терминал и перейдите в вашу домашнюю директорию. Скачайте vcpkg с официального GitHub и запустите его внутренний скрипт сборки:

    cd ~
    git clone https://github.com
    cd vcpkg
    ./bootstrap-vcpkg.sh

После этого в папке vcpkg появится готовый исполняемый файл. Чтобы система всегда знала, где он лежит, добавьте путь к нему в переменную окружения.

    export VCPKG_ROOT=$HOME/vcpkg
    export PATH=$VCPKG_ROOT:$PATH

------------------------------
### Шаг 2. Скачиваем библиотеку ИИ
Теперь, находясь в консоли, дайте команду vcpkg найти, скачать и скомпилировать библиотеку ONNX Runtime специально под вашу операционную систему:

    vcpkg install onnxruntime

Процесс может занять несколько минут, так как vcpkg сам скачает исходный код, проверит зависимости и соберет файлы .a или .dylib, которые мы будем подключать к С++.
------------------------------
### Шаг 3. Собираем наш медицинский проект через CMake
Создайте новую папку для вашего проекта, например HolterProject. Положите туда ваш файл с кодом main.cpp (где прописаны классы ридера, ИИ, визуализатора и обработки ошибок) и созданный ранее файл CMakeLists.txt.
Теперь запустите сборку, явно указав CMake, что нужно использовать менеджер пакетов vcpkg. Для этого передается специальный флаг CMAKE_TOOLCHAIN_FILE:

    #### 1. Заходим в папку с проектом
    cd ~/HolterProject
    #### 2. Создаем отдельную папку для сборки (чтобы не мусорить в коде)
    mkdir build && cd build
    #### 3. Конфигурируем проект (CMake сам подтянет ONNX через vcpkg)
    cmake .. -DCMAKE_TOOLCHAIN_FILE=$HOME/vcpkg/scripts/buildsystems/vcpkg.cmake
    #### 4. Компилируем финальный исполняемый файл
    cmake --build .

------------------------------
### Финал: запуск программы
После успешной компиляции в папке build появится ваш готовый файл. Положите рядом с ним скачанный файл модели mit_bih_arrhythmia.onnx и файл с ЭКГ patient_data.txt, после чего запускайте:

./holter_app

Терминал  превратится в диагностический центр, выдавая логи ошибок, процентные вероятности ИИ и рисуя символьные графики ЭКГ прямо на экране.
Вы полностью развернули рабочую среду и написали каркас серьезного продукта. С этого шага проекты официально превратились в реальные инженерные навыки.
